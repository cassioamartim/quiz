plugins {
    id 'java'
}

group 'io.github.cassioamartim'
version '1.0-SNAPSHOT'

compileJava.options.encoding('UTF-8')

repositories {
    mavenCentral()
}

dependencies {
    def lombok = 'org.projectlombok:lombok:1.18.26'

    compileOnly(lombok)
    annotationProcessor(lombok)

    compileOnly files('C:/API/PaperSpigot.jar')
}